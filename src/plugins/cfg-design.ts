import { setTabsConfigs } from '@/uni_modules/cfg-design'

setTabsConfigs({
  equal: {
    item: { activeType: 'bg', cStyle: { width: '100%' } },
    cStyle: { padding: '14rpx 20rpx' },
    cClass: ['c-tabs-equal']
  }
})
